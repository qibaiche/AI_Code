# 测试说明

## 测试文件

### test_recent_fixes.py

验证最近的修复和新功能。

**测试内容：**
1. GTS 文件查找功能
2. 截图功能
3. 等待工具
4. 错误处理机制

**运行方法：**
```bash
cd automation/auto-vpo
python tests/test_recent_fixes.py
```

**预期输出：**
```
╔══════════════════════════════════════════════════════════════════════════════╗
║                    Auto-VPO 最近修复验证测试                                 ║
╚══════════════════════════════════════════════════════════════════════════════╝

================================================================================
测试 1: GTS 文件查找功能
================================================================================

场景 1: 在当前运行目录中查找
✅ 找到文件: GTS_Submit_filled_20251226_025705.xlsx

场景 2: 在父级 output 目录中查找
✅ 找到文件: GTS_Submit_filled_20251226_025705.xlsx
   完整路径: C:\...\output\GTS_Submit_filled_20251226_025705.xlsx

场景 3: 在新建的空目录中查找
✅ 找到文件: GTS_Submit_filled_20251226_025705.xlsx
   文件位置: C:\...\output
   已清理测试目录

================================================================================
GTS 文件查找测试完成
================================================================================

... (其他测试)

╔══════════════════════════════════════════════════════════════════════════════╗
║                              测试总结                                        ║
╠══════════════════════════════════════════════════════════════════════════════╣
║  通过: 4  失败: 0  总计: 4                                                   ║
╚══════════════════════════════════════════════════════════════════════════════╝

🎉 所有测试通过！
```

## 依赖项

测试脚本需要以下依赖：
- Python 3.7+
- Pillow (可选，用于截图测试)

安装依赖：
```bash
pip install Pillow
```

## 注意事项

1. **GTS 文件查找测试**
   - 需要 `output` 目录中存在至少一个 `GTS_Submit_filled_*.xlsx` 文件
   - 如果没有，测试会失败

2. **截图测试**
   - 如果没有安装 Pillow，截图测试会跳过
   - 测试会自动清理生成的截图文件

3. **等待工具测试**
   - 会有短暂的等待时间（几秒）
   - 这是正常的，用于测试超时机制

4. **错误处理测试**
   - 会故意抛出异常来测试错误处理
   - 日志中的错误信息是预期的

## 添加新测试

要添加新的测试，在 `test_recent_fixes.py` 中添加新的测试函数：

```python
def test_new_feature():
    """测试新功能"""
    print("\n" + "=" * 80)
    print("测试 X: 新功能")
    print("=" * 80)
    
    # 测试代码
    try:
        # 执行测试
        result = some_function()
        
        if result:
            print("✅ 成功")
        else:
            print("❌ 失败")
    except Exception as e:
        print(f"❌ 失败: {e}")
    
    print("\n" + "=" * 80)
    print("新功能测试完成")
    print("=" * 80)
```

然后在 `main()` 函数的 `tests` 列表中添加：

```python
tests = [
    # ... 现有测试
    ("新功能", test_new_feature),
]
```

## 持续集成

未来可以将这些测试集成到 CI/CD 流程中：

```yaml
# .github/workflows/test.yml
name: Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.9'
      - run: pip install -r requirements.txt
      - run: python automation/auto-vpo/tests/test_recent_fixes.py
```

